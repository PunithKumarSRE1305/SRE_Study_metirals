# Interview seeds — Kubernetes

- What happens when a Pod starts? When it dies?
- How a Service finds a Pod. What if DNS fails?
- How Kubernetes decides a container is unhealthy.
- What a rolling deploy actually does. What if a node disappears?
- What if the control plane is unavailable — what still runs?

Opens after Month 18. Depth over `kubectl` trivia.
