# Interview seeds — Linux

Not a study plan. Seeds only.

- What happens when you type `cd /var/log`?
- Why might `df` and `du` disagree?
- SIGTERM vs SIGKILL. When does `-9` fail?
- How do you investigate load average 40 on a 4-CPU box?
- A process is in `D` state. What now?

Full sessions: Month 31.
